### Test Environments

- `jest@27` support test environment using typescript. But the ts config paths cannot be resolved.
- So we put the `test-environments` in `src` directory and compile to js before runing test
